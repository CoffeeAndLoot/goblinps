# SDD ledger — plan: docs/superpowers/plans/2026-09-20-goblinps-dash-unit.md

Spec: docs/superpowers/specs/2026-09-19-goblinps-design.md (decisions 3, 5, 7). Branch: dash-unit, from main 74606ed.

Ruling: plain branch, not a worktree — the game junction targets D:\goblinps\GoblinPS and follows whatever is checked out, so a worktree would hide the work from the client the user tests in. Same ruling as plans 2 and 3 — if wrong: a worktree can be made later; nothing depends on the layout.

## Pre-flight scan

| Pair / task | Produces vs consumes | Found |
|---|---|---|
| T1 -> T6 | `ns.Data.Art[name] = { file, l, r, t, b }` | shapes match; T6 reads exactly those five fields |
| T1 -> T4 | shipped textures in Media | T4 deliberately uses WHITE8X8 and reads no Art; no coupling |
| T2 -> T5 | Bearing, ArrowAngle, DistanceTo, Remaining | all four defined in T2 and called in T5 with matching arity |
| T3 -> T5 | API.PlayerFacing, API.OnTaxi, API.OnTripEvent, Core.Here | names and returns agree |
| T4 -> T5 | Dash.Start/Stop/Refresh/Debug, `ui.step/next/distance/eta/arrow` | agree |
| T4 -> T6 | `ui` gains bodyArt, screenArt, compass, plateArt | agree after the self-review fix that added the ETA plate |
| T5 -> T6 | T6 edits `aimArrow` to also turn the compass | T5 defines `aimArrow`; edit point exists |
| T3 -> T4/T6 | the fake frame library | **P1, P2, P3 below** |
| T5 -> test file | the pin test reads `pins` | **P4 below** |
| T1 self | tests vs code | `PARTS`, `SOURCE`, `next_power_of_two`, `tex_coords` all referenced by the tests are defined |
| T2 self | tests vs code | consistent; needs `Trip` in scope, which the brief says to take as the file already does |
| T3 self | tests vs code | consistent |
| T4 self | tests vs code | `fs.points` is real (fake_frames.lua:46, 96); anchors test will work |
| T5 self | tests vs code | consistent apart from P4 |
| T6 self | tests vs code | consistent apart from P1 |
| T7 self | docs only | no code |

**P1. `SetTexCoord` is accepted and ignored.** `test/fake_frames.lua:17` lists it among methods that are swallowed, so `texture.texCoord` is never recorded. Task 6's first test reads `texture.texCoord[1]` and would fail on a nil index, not on the behaviour under test.

**P2. `SetVertexColor` is not modelled at all.** The fake is strict: an unmodelled PascalCase method raises. Task 4's `arrow:SetVertexColor(unpack(W.COLOR.green))` would raise on the first run.

**P3. `SetDrawLayer` is not modelled either.** Task 6's `eta:SetDrawLayer("OVERLAY")` would raise the same way.

Ruling: P1, P2 and P3 are all fixed in **Task 3**, which already opens `test/fake_frames.lua` to add `SetRotation`. Recording a texture's coordinates, vertex colour and draw layer is the same one-line-per-method change, and doing it in one visit keeps the fake's diff in one review. — if wrong: three one-line additions move to whichever later task trips over them, costing one fix round each.

**P4. A separate `test/test_dash.lua` would break `test/test_ui.lua`.** Suites are loaded and called at load time and only run later, and `Fake.Install()` reassigns globals wholesale, including `_G.SlashCmdList = {}` and `_G.print`. A second suite installing the fake would detach test_ui's `printed` table and wipe its registered slash command **after** test_ui had set itself up, so test_ui's tests would then run against a different environment. Task 5's pin test also reads `pins`, which exists only inside test_ui's scripted API.

Ruling: the dash tests go in **`test/test_ui.lua`**, not a new `test/test_dash.lua`. That file already installs the fake exactly once, scripts `ns.API`, records `pins` and `printed`, and loads the UI modules into its own namespace; `Dash` joins its module list and the dash state (`facing`, `onTaxi`, `where`) joins its locals beside `level`. Tasks 4, 5 and 6 are amended accordingly, and `test/run.lua` gains no new suite. — if wrong: test_ui.lua grows from 487 to roughly 750 lines and splitting it later is mechanical, whereas getting this wrong now silently corrupts an existing passing suite.

Consequence of P4 for the briefs: wherever tasks 4, 5 and 6 say "Create `test/test_dash.lua`" or "Append to `test/test_dash.lua`", read "add to `test/test_ui.lua`". The `ns.Test` seam the plan describes for task 5 is unnecessary there: test_ui already owns `where` through its scripted `PlayerMapPosition`, and `Core.Here` reads through it. Task 5's dispatch carries this.

Also confirmed present and usable, so no task needs to add them: `Region:Show`, `Hide`, `IsShown` (fake_frames.lua:55-61), `StartMoving`/`StopMovingOrSizing`, `SetSize`, `Fake.missingTextures`, and `points` for the anchor check.

Scan complete: 4 findings, all ruled on above.

## Progress
Task 1: implemented, but the controller fouled the commit. I ran `git commit` for the plan corrections while the implementer had its own files staged, so a bare commit swept Task 1's entire deliverable into `d0a04c6` under a message about the plan. Split with `git reset --soft` into `4fbcc68` (plan) and `a667353` (Task 1); the tree SHA `abcde473...` is byte-identical before and after, so nothing was lost. Nothing had been pushed.

Ruling: from here on the controller commits only with explicit paths (`git commit ... -- <paths>`) and never with a bare `git commit` while any subagent is live. — if wrong: nothing; it is strictly safer and costs one extra argument.

Ruling: Task 2 dispatched while Task 1's review is still running. They are disjoint — Task 1 owns `tools/`, `test/tools/`, `GoblinPS/Data/Art.lua` and `GoblinPS/Media/`; Task 2 owns `GoblinPS/Trip.lua` and `test/test_trip.lua` — and only one of the two live agents commits, so the race that caused the commit above cannot recur. Precedent: plan 3 dispatched Task 3 in parallel on the same grounds. — if wrong: a conflicting edit costs one fix round, and the two file sets do not overlap at all.

Task 1: controller sanity check before review: Python 27/27, `Art.lua` holds the five rows in the specified shape, shipped textures 1.2 MB including the pre-existing icon, `images/parts/*.tga` still untracked (0 tracked). Review dispatched (sonnet).
Task 1: review (sonnet) — **spec ✅, quality approved**, 0 Critical, 0 Important, 1 Minor. Reviewer verified independently rather than from the diff alone: boundary values of next_power_of_two, tex_coords with unequal ratios on both axes, a loud FileNotFoundError when a source PNG is moved aside (and no partial write, since the table is written only after the loop), determinism by re-running the tool against the committed files, and a Pillow check that dash-body.tga is 256x256 RGBA and not the 1024px source. Python 27/27, check_art 39 pass. It also named a falsifying mutation for each of the nine new tests; none is vacuous.
Task 1: minor (resolved, not deferred): the implementer's report claimed another agent had committed the work, and named the dangling d0a04c6. That was the controller's bare-commit mistake, already recorded above; a controller note correcting the audit trail is appended to task-1-report.md.
Task 1: ⚠️ cannot-verify item — in-game rendering and alignment of the three concentric layers. Not a gap: no client exists in this environment by design, and Task 7 puts it on docs/manual-test-checklist.md as an in-game item. Resolved.
Task 1: complete (commits 4fbcc68..a667353, review clean)
Task 2: review (sonnet) — **spec ✅, quality needs fixes**: 2 Important, 1 Minor. Reviewer re-derived the world frame from Geo.lua AND tools/build_graph.py's inverse before judging the maths, hand-worked all four branches of Trip.Remaining against a scratch script, and named a falsifying mutation for all eleven new tests.

Task 2: review finding 2 is **plan-mandated and the plan is wrong**. `Trip.Remaining`'s final expression, copied verbatim from the brief, gives a ride step with no known position 0 seconds instead of its planned time: `Trip.Remaining(result, 1, nil, 7)` returns 290 where the honest answer is 390. That is exactly the loading-screen or instance case `Trip.Check` calls "pause", so a device built on it would show the current leg as already arrived.

Ruling: the finding wins over the plan text. The spec (decision 3) wants an honest time left, and zero-when-unknown is not honest. The fix also simplifies: `d and d / speed or step.seconds or 0` drops the special case entirely, because Lua treats 0 as truthy so a ride standing exactly on its target still yields 0 through the `d and d / speed` arm. Verified against the existing tests: ride-with-position 300 unchanged, last-step-at-target 0 unchanged, non-ride 290 unchanged, ride-without-position becomes 390. — if wrong: one line and one test; the alternative is a dash unit that lies about the leg you are on whenever the client drops your position.

Task 2: review finding 1 (Important): Trip.DistanceTo has no direct tests at all; its cross-continent and nil guards are reachable only indirectly and no existing test distinguishes them. Reviewer proved the gap by tracing every call site rather than asserting it. Goes into the same fix round.

Task 2: minor (deferred, no action): the implementer's report says 196 tests where HEAD now shows 198. Not a defect — Task 3's implementer committed concurrently. The count in a task report is true of its own commit, not of a moving branch tip.

Note for Task 3's review: the language server currently flags `Core.Here` calling an undefined global `here` (GoblinPS/Core.lua:43). The controller's dispatch told it to put the accessor "near the top with the other accessors", but `here` is a `local function` defined further down, so at line 43 the name is not yet in scope. Controller instruction defect, not the implementer's. Task 3's own zero-warning gate should catch it; if it does not, this is a Critical for its review.
Task 2: fix round 1/5 dispatched (resumed the original implementer), FIX_BASE ed960d0
Task 2: fix round 1/5 applied (commit f9f3fd2, parent 8d68010, touches only Trip.lua and test_trip.lua). Controller verified the five cases by running them before dispatching the re-review: ride with position 300, ride with no position 390, ride with a cross-continent position 390, zeppelin 290, last step standing on target 0. The last is the one that could have broken: 0 is truthy in Lua, so a leg exactly on target still returns 0 through the distance arm rather than falling through to planned time. Scoped re-review dispatched (haiku), told to prove the new DistanceTo tests bite by deleting the guard and watching the suite fail.
Task 3: implemented 8d68010 (DONE). Two deliberate deviations from the brief, both of which look right and both of which are controller defects the implementer caught: (1) the brief said to put Core.Here "near the top with the other accessors", but `here` is a local function declared further down, so that placement closes over a nil global; the implementer proved it with a call that errored and moved the accessor below the local. (2) the brief gave the three new fake-frame methods as comma-terminated table entries, which is a syntax error in a file that uses `function Region:Name()`; the implementer used the file's style. Review dispatched (sonnet) and told to judge both deviations independently rather than assume they are right.
Task 4: implementer dispatched (sonnet), BASE 8d68010. Disjoint from the live Task 2 fix (Trip.lua, test_trip.lua) and from the read-only Task 3 review.
Task 3: review (sonnet) — **spec ✅, quality approved**, 0 Critical, 0 Important, 1 Minor. Both deliberate deviations verified independently and upheld: the reviewer loaded the committed Core.lua and called Core.Here() successfully, AND reproduced the failure mode the brief's literal wording would have caused. It proved SetTexCoord now records (0,0.75,0,0.5) and that the accepted-and-ignored entry is gone, and proved the fake still raises on an invented method, so the strictness that caught these defects is intact. Event mapping checked for inversion and is correct: only PLAYER_CONTROL_GAINED maps to "landed". luacheck 0 in 38 files, language server empty JSON.
Task 3: minor (deferred): no Lua test exercises the real API.lua functions against stubbed globals; only test_ui's hand-written ns.API stand-in gained entries. Not a deviation — the brief asked for exactly that — and it matches the project's standing convention in CLAUDE.md that pure logic is unit-tested while frames and live game data are verified in game, API.lua being the boundary. The four event names are the risk here and they are pinned by the research doc and by review, not by a test. Noted for the final review's triage.
Task 3: complete (commits ed960d0..8d68010, review clean)
Task 2: fix round 1/5 re-review (haiku) — both findings ADDRESSED, no new breakage. Reviewer measured all five Remaining cases and proved the new DistanceTo tests bite by mutating the guard to `return d`, which failed with `expected nil, actual inf` on the cross-continent case, then restored the file and confirmed a clean git status.
Task 2: complete (commits a667353..f9f3fd2, 1 fix round, re-review clean)
Task 4: review (sonnet) — **spec ✅, quality approved**, 0 Critical, 0 Important, 1 Minor. Reviewer instrumented CreateFrame and proved build() runs exactly once across two Start calls (frame count stayed at 3, and Debug()'s ui table was identical by identity), proved Stop-then-Start works without rebuilding, checked the historical relativePoint drag bug is absent, tabulated all four FontStrings against the two-anchor rule, and confirmed no Task 5 work leaked in (no Tick, no OnUpdate, no RegisterEvent). It also verified Data\Art.lua had been orphaned since a667353 and is now wired into the TOC.

Task 4: minor, NOT deferred — carried into Task 5 as a requirement. Calling `Dash.Start` with a zero-step plan **while a trip is already open** returns early and leaves the previous trip's text on screen: the reviewer measured `frame:IsShown()` still true with "Ride to the North Gate" still showing. The committed test only covers the closed-window path. This is a gap in the brief's own prescribed code, not an implementer deviation, and Task 4's scope explicitly excludes trip logic.

Ruling: the fix belongs in Task 5, not a Task 4 fix round. Task 5 owns recalculation, and the only way to reach a zero-step plan with the window open is a recalculation that finds you already there — at which point the honest behaviour is to finish the trip, not to blank the text. Task 5's dispatch carries this as a required case with its own test. — if wrong: a stale line of text in a corner case, fixable in one round; whereas opening a Task 4 fix round would edit Dash.lua while Task 5 rewrites it.
Task 4: complete (commits f9f3fd2..b7d3fb3, review clean)
Task 5: implementer dispatched (sonnet), BASE b7d3fb3. Serialised on purpose: it rewrites Dash.lua, so nothing else runs against that file.
Task 5: implemented d3548db (DONE_WITH_CONCERNS in substance; the agent reported four departures from the brief). 219 Lua tests. Controller checks before dispatching the review:
  - Independently ran both linters at this commit: luacheck 0 warnings / 0 errors in 39 files, lua-language-server "no problems found" with an empty [] diagnostics file. The IDE's live analysis disagreed and flagged cast-local-type in test_ui.lua and undefined-field in Planner.lua. CLAUDE.md defines the gate as the CLI `lua-language-server --check`, so the CLI is authoritative and the task passes; the IDE and the CLI evidently load .luarc.json differently. Noted for the final review rather than acted on.
  - Verified mechanically that the implementer's claim "no assertions changed" holds: no `h.it(` line was removed or renamed (nine added, none deleted), and no `h.eq`/`h.truthy`/`h.falsy` line was deleted anywhere in the diff. The only deletions in test_ui.lua are the three fixture rows it re-added with `map` fields. The reviewer is asked the subtler question the mechanical check cannot answer: whether the two pre-existing planner tests still MEAN what they meant now that GO closes the window.
Task 5: review dispatched (sonnet), told to hand-walk all four Trip.Check verdicts, prove the event registers once and state.best clears at both sites, and drive a whole journey in a scratch script rather than trusting the committed tests.
Task 5: review (sonnet) — **spec ✅, quality approved**, 0 Critical, 0 Important, 1 Minor. The reviewer drove a whole journey in a scratch script and printed state.best at each stage: 5000 -> 700 -> nil on arrival -> 7071 while straying on a zeppelin (correctly no advance, not a ride step) -> 0 aboard a taxi (onTaxi guard held) -> nil on the next arrival -> finish. It confirmed the event registers once at Dash.lua:94 inside build(), which is itself guarded by `if not ui`, and listed all three state.best reset sites (150, 180, 188). It hand-walked all four Trip.Check verdicts and found each mapped to exactly one branch. It also exercised the NON-zero-step recalculation path, which no committed test reaches, and watched the plan replace correctly.
Task 5: all four departures from the brief upheld. The fixture edit is justified (Core.PinStep bails on a missing step.to.map, and Graph always sets map on every stop, so the enriched fixture is more realistic, not a special case). The two modified planner tests still mean what they meant: one gained a strengthening assertion, and the other's assertions read FontStrings that Planner.Refresh writes unconditionally with no visibility gate, so hiding the window does not make them vacuous. The type annotation is a comment Lua 5.1 ignores. The zero-step recalculation routes into finish() as required.
Task 5: minor (deferred): Dash.Tick treats any unrecognised verdict as "stay" by falling through rather than matching the string, so a typo'd verdict from Trip.Check would be silently absorbed instead of surfacing. Correct today for Trip.Check's four documented returns. Noted for the final review's triage.
Task 5: complete (commits b7d3fb3..d3548db, review clean)
Task 6: implementer dispatched (sonnet), BASE d3548db. Serialised: it edits Dash.lua.
Task 6: implemented f597b4f, 221 tests. The implementer found two defects in the brief and fixed both. The second matters: **the brief's missing-texture fallback test was vacuous.** build() runs once and Stop/Start does not rebuild, so setting Fake.missingTextures after the window existed never exercised the fallback at all — the test would have passed forever while protecting nothing, which is precisely the rule it was written to guard (art over colours so a renamed texture cannot break the window). It rewrote the test to load an independent second copy of Dash.lua, added an h.falsy(ui.bodyArt) assertion, and proved it by reintroducing the bug. The reviewer is told to repeat that mutation independently rather than accept the claim. It also found test_ui.lua never loaded the generated Data/Art.lua, so the brief's literal test would have errored on a nil index.
Task 6: review dispatched (sonnet).
Task 7: implemented 7b35aa7, docs only. Controller checked before review: the `## Dash unit (plan 4)` section exists with exactly 13 items, and Trip.ROTATION_SIGN is named in the checklist as required so the in-game fix is one character. Review dispatched (sonnet), told that documentation describing behaviour the code does not have is worse than none, and to check every factual claim against source.
Task 7: review (sonnet) — **spec ✅ (13/13 items verbatim and in order), quality needs fixes**: 1 Critical, 1 Important, 1 Minor. The reviewer checked eleven factual claims against source in a table and found two that do not hold.

Task 7 Critical: the spec's decision 5 says tools/make_art.py scales and pads the gitignored authoring TGAs in images/parts/. It does not — make_art.py:64 reads the PNGs. The TGAs there are a separate regenerable review export from images/parts/export_tga.py, and the two TGA sets come from unrelated scripts. Straightforwardly false and goes into the fix round.

Task 7 Important: checklist item 10 promises "it says Arrived and the device closes", but Dash.lua's finish() sets the text and clears state without ever hiding the frame; only Dash.Stop() at line 161 hides it. The plan was internally inconsistent — my checklist text and my task 5 code disagreed — and the implementer transcribed both faithfully.

Ruling: **the code is right and the checklist is wrong.** A device that vanishes the instant you arrive is worse than one that says so and waits: the confirmation is the point, and arrival is exactly the moment a player's attention is on the world rather than the window. There is already a Stop button to dismiss it, and Dash.Stop() clears state properly. Fix the wording, not the behaviour. — if wrong: one line of checklist text, versus changing shipped behaviour on a guess about a window nobody has seen in game yet.

Task 7 Minor: the spec's top status line still lists "4 dash unit" without the "(built)" marker that plans 2 and 3 carry, now contradicting decision 3 a few lines below, which this same diff rewrote to describe the dash unit as built. Folded into the same round since it is one line in a file already being edited.
Task 7: fix round 1/5 dispatched (resumed the original implementer), FIX_BASE 7b35aa7
Task 7: fix round 1/5 re-review (haiku) — all three findings ADDRESSED, no new problems, no code touched, 221 tests.
Task 7: complete (commits f597b4f..5ddefe4, 1 fix round, re-review clean)

Task 6: review (sonnet) — **spec ✅, quality needs fixes**: 1 Critical, 1 Important. Both claimed brief defects upheld; the reviewer independently reproduced the vacuous-test mutation (220 passed / 1 failed at the added h.falsy(ui.bodyArt) line) and restored the file. It then measured alpha in the generated TGAs and reasoned about WoW compositing to raise a Critical the desktop tests structurally cannot catch, because the fake does no rendering.

Task 6 Critical, confirmed by the controller reading build() directly. The brief's layering is wrong three ways, all from mixing child frames with draw layers:
  1. `bodyArt` is on `f` at OVERLAY and created AFTER the four FontStrings, which Widgets.Text also puts on OVERLAY of `f`. Same frame, same layer, later creation wins, so the body draws over the directions.
  2. `screen` is a CHILD frame of `f`, and a child outranks every layer of its parent, so screenArt/compass/arrow all draw ABOVE bodyArt. The bezel that exists to sit on top with a hole in it actually sits underneath.
  3. `plate` is a child frame too, so plateArt (BACKGROUND on a child) draws above the `eta` FontString (OVERLAY on the parent). The plate covers the text it is meant to back.

Ruling: fix it, do not park it for the in-game pass. The reviewer is right that only the client can confirm pixels, but the defect is not a pixel question — it is that the code depends on WoW's default child-frame level and on creation order, neither of which is stated anywhere and both of which the fake silently swallows (SetFrameLevel is in fake_frames' accepted-and-ignored list, line 21). Making every level explicit turns an invisible assumption into a stated one that a test can pin, which is worth doing whether or not the current art happens to overlap. — if wrong: a restructure of build() that the in-game pass would have caught anyway, against shipping a device whose text is hidden behind its own bezel.

Task 6 Important: nothing asserts the compass's rotation. Changing `ui.compass:SetRotation(-(facing))` to use the arrow's target-relative angle would pass the whole suite. Folded into the same round.
Task 6: fix round 1/5 dispatched (resumed the original implementer), FIX_BASE f597b4f
Task 6: fix round 1/5 re-review (sonnet) — both findings ADDRESSED, no new breakage. Reviewer traced the whole stack with line numbers (base at Dash.lua:88, screen base+1, bezel base+2, content base+3, stop base+4, all derived from f:GetFrameLevel()), confirmed plateArt sits below the eta text by layer and above bodyArt by frame level, and confirmed no anchor was lost in moving the FontStrings between frames. It proved the compass test bites by coupling the compass to the arrow's angle and watching it fail with expected 0 / actual 1.5707963267949, then restored the file and re-ran clean. It verified the fake's GetFrameLevel recurses to parent-plus-one and that strictness survives the ignored-list removal.
Task 6: the reviewer corrected an overclaim in the controller's framing of the Stop button. Neither bezel nor content calls EnableMouse, and plain textures never intercept clicks in WoW, so the button was always clickable — it was merely invisible under the full-frame body art. A visual fix, not a functional one. Worth recording because the wrong version of that claim would have overstated the severity of what shipped.
Task 6: complete (commits d3548db..61583d0, 1 fix round, re-review clean)

All seven tasks complete. Dispatching the final whole-branch review.

## Final whole-branch review (opus): "ready with fixes" — 0 Critical, 4 Important, 6 Minor, 3 Nit
All gates green. It confirmed make_art.py regenerates Data/Art.lua and the five TGAs byte-identical, so nothing generated was hand-edited. All four Important findings came from driving the assembled device, not reading it, and all four are defects in the plan rather than in any implementer's transcription:
  I1. "Recalculating..." never reaches the screen: Dash.lua:263 writes it and Dash.lua:264 calls Refresh(), which overwrites ui.next unconditionally in the same Tick. Proved with a sentinel across a real stray. Spec decision 3 requires the message and checklist item 6 tests for it.
  I2. The map pin is not moved after a recalculation. advance calls Core.PinStep and Core.Go pins step 1, but the recalculate branch pins nothing; measured as pin count stuck at 1 across a replan that provably swapped the plan.
  I3. The compass ignores Trip.ROTATION_SIGN, hard-coding -(facing) while the arrow goes through ArrowAngle which multiplies by it.
  I4. A failed arrow texture destroys its own fallback: the generated path overwrites WHITE8X8 whether or not it loads, and only the tex-coords and tint are guarded. The art() helper gets this right for every other part; the arrow is the one element with no colour behind it.

Ruling: I3 is the finding to lead on, and the reviewer is right that it outranks the others despite being equal in severity. The other three are ordinary bugs that cost a session. I3 corrupts the in-game pass itself: the checklist hands the user a one-character remedy for a mirrored arrow, and following it would fix the arrow and silently break the compass, so the session would end with a wrong conclusion recorded as a verified fact. A defect that produces false evidence is worse than one that produces a wrong pixel. — if wrong: one line in Dash.lua either way.

Deferred findings from earlier tasks: both stand, neither blocks. The reviewer retired Task 3's concrete risk with evidence rather than a test, confirming all four event names appear in Blizzard's own RegisterEvent calls in the local forever clone at D:\wow-api\1.60.1.69913 and that GetPlayerFacing is Nilable in the generated docs. Task 5's fall-through is the safer default mid-journey and Trip.Check has exactly four returns, all handled once.
Final review: one fix wave dispatched (the only one; there is no second), FIX_BASE 61583d0
Final fix wave: applied in 1b95ace and 0068186, 235 Lua tests (+12, each RED then GREEN), 27 Python, 39 art parts, both linters clean. Scoped re-review (sonnet): **every finding ADDRESSED**, no functional breakage.
  I3 verified by mutation, which is the one that mattered: with ROTATION_SIGN at -1 the arrow AND the compass both flipped together, and the two resulting failures were pre-existing tests hard-coding the default value, not a disagreement between the two rotations. Restored and re-confirmed 235/235. The checklist's promise that flipping one constant is the whole remedy is now true.
  M4 verified by actually moving Data/Art.lua aside and loading the addon: no error, SelfTest still a function, file restored.

Residual, PARKED. The reviewer found the implementer's own disclosed defect class in two more places it had not fixed: test_ui.lua:787 and :801 each restore the shared `where` fixture on a bare final line, after several assertions, with no pcall. A failing assertion earlier in either test would skip the restore and corrupt the fixture for everything after. The reviewer proved empirically that it does not cascade today — it injected a failure and saw 234/235 with no knock-on, because the next test resets `where` unconditionally at its own start — so it is latent fragility, not an active fault.

Ruling: park it rather than open a second fix wave. The process allows exactly one fix wave and one scoped re-review, and the controller does not fix findings itself because controller fixes skip review entirely. The defect is in test code, is provably non-cascading against the current suite, and the remedy is to wrap two restores in pcall exactly as the third already is. It goes to the user as a known residual and to the next plan as a first task. — if wrong: a future failing assertion in one of those two tests produces a confusing cascade in unrelated tests, which is annoying to diagnose but cannot reach shipped behaviour.

All seven tasks complete. Final review clean after one fix wave. Branch ready to hand to the owner.
